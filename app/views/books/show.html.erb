<%= render 'books/user_info', book: @newbook %>
<h2>Book detail</h2>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td></td><!--仮置-->
      <td><%= link_to @book.title, book_path(@book) %></td>
      <td><%= @book.body %></td>
      <td><%= link_to "edit", edit_book_path(@book) %></td>
      <td><%= link_to "destroy", book_path(@book), method: :delete %></td>
    </tr>
  </tbody>
</table>